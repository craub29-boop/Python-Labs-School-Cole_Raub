import random as rng
from turtle import distance

def check_if_number(num):
    try:
        float(num)
        return True
    except ValueError:
        return False

def take_input():
    global num
    global guess_amount

    while True:
        guess = input("guess a number between 1 and 10: ")
        if check_if_number(guess):
            guess_amount += 1
            return int(guess)
        print("Please enter a valid number.")

def rng_number():
    global num
    return rng.randint(1, 10)

def start_game():
    global guess_amount
    global num
    print("Hola")
    print("I will ask you to guess a number between 1 and 10.")
    guess = "test1"
    num = int(rng_number())
    guess_amount = 0


def check_guess(guess, num):
    global guess_amount

    distance = guess - num
    if distance == 0:
        print("correct")
        print("It took you " + str(guess_amount) + " tries.")
        if guess_amount <= 3:
            print("noice")
        elif guess_amount <= 5:
            print("meh")
        else:
            print("bleh")
        guess_amount = 0
        quit()
        return True
    return False

def above_or_below(guess, num):
    distance = num - guess
    if distance > 0:
        print("higher")
    elif distance < 0:
        print("lower")


def main():
    global num
    global guess_amount
    start_game()
    while True:
        guess = take_input()
        above_or_below(guess, num)
        if check_guess(guess, num):
            num = int(rng_number())


if __name__ == "__main__":
    main()

