import time 
import random as rand

def explain():
    print("first type in guess, then type in color")
    print("type y for yellow, g for green, and gr for gray")


#takes and filters word input 

def word_input():
    user_input = input("input: ").strip().lower()

    x = 0

    if user_input == "":
        quit()
    elif len(user_input) != 5:
        print("enter a 5-letter word")
        return False

    while x < len(user_input):
        if user_input[x] < 'a' or user_input[x] > 'z':
            print("enter leters only")
            return False
        else:
            x += 1
    return user_input

#takes and filters color input

def take_color():
    color_input = input("color: ").strip().lower()
    if color_input == "":
        quit()
    elif color_input not in ["y", "g", "gr"]:
        print("enter valid color")
        return False 
# 3 lists, green, yellow, gray 
def assign_list(color, guess):
    global green_list
    global yellow_list
    global gray_list

    green_list = []
    yellow_list = []
    gray_list = []

    for i in range(len(guess)):
        if color[i] == "g":
            green_list.append(guess[i])
        elif color[i] == "y":
            yellow_list.append(guess[i])
        elif color[i] == "gr":
            gray_list.append(guess[i])

def gen_rand_char():
    return chr(rand.randint(97, 122))
class Guess:
    def __init__(self, word, color1, color2, color3, color4, color5):
        self.word = word 
        self.color = [color1, color2, color3, color4, color5]


guess1 = Guess( word_input(), take_color(), take_color(), take_color(), take_color(), take_color())

assign_list(guess1.color, guess1.word)

print(green_list)